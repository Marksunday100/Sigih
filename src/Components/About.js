const About = () => {
  return (
    <div className="about">
      Shortener is a link shortner which you can use to shorten your long URLs.
      When pasting links, make sure it is a url link and make sure the link is
      also a public link.{" "}
    </div>
  );
};

export default About;
